# Notable quotes: Building with Claude on Google Cloud

Quotes selected by Claude from the transcript.

> How many of you used an AI coding tool this week? Raise your hand. The majority. Sounds good. How many of you used the same coding tool to build and deploy something on Google Claude? Raise your hand. Okay. Not that bad, but we can do better.

*Establishes the talk's premise and identifies the gap between AI tool usage and actual deployment on Google Cloud.*

> Claude Code, the Anthropix coding agent, provides a set of capabilities that will essentially augment them across this entire software lifecycle.

*Core thesis statement about Claude Code's role in augmenting development teams throughout the software lifecycle.*

> If you use Claude models on Google Claude, you pay per token. So you don't receive any message per cap. And if you need some additional capacity to run this model, because maybe you're building a production application, you can always get access to what is called provisioning throughput.

*Key value proposition differentiating Google Cloud's Claude hosting from other platforms.*

> Google Claude spent a considerable amount of time to integrate with the open bytecoding ecosystem that is fostering around Claude. And we introduced two important things. One is the official Google Claude skills. And the other one is the developer knowledge API with the associated MCP server.

*Highlights Google Cloud's strategic investment in Claude integration and the key technical components that enable it.*

> With the developer knowledge API, you will be able, together with Claude, to say, OK, I can deploy the API on a serverless function on Claude Run. Then I can attach a dedicated DB for website, like file stores.

*Demonstrates how the developer knowledge API enables architectural decisions without prior Google Cloud expertise.*

> I will use another component of Claude Code, which is represented by the subagents. In this way, I can parallelize tasks, simulating like a team sprint. And I will have one subagent for the API, one for the data pipeline in BigQuery, and the other one for the dashboard.

*Shows how Claude Code's subagents feature can simulate parallel development work typical of software teams.*

> With the planning mode, this mode puts Claude in a mode where it thinks and it starts coding. And this is very important because it gives me some degree of freedom to decide what to build before Claude start building, based on my personal preference.

*Explains the value of planning mode in providing developer control over the code generation process.*

> You can use the second MCP server, which is a couple of days later, and you can send it back. And you can send it back to the other server. And you can send it back to the other server. server that it will use those numbers to create a dashboard in the Google Claude dashboard tool which is Looker.

*Demonstrates the power of chaining MCP servers to create complex workflows from data analysis to visualization.*
